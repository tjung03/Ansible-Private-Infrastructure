---
# 4) 방화벽 등록
# firewalld에 http 서비스를 등록한다.
# firewalld 설치 및 기동은 별도 firewall role에서 처리한다.

- name: 4-1) firewalld http 서비스 등록 - {{ web_firewall_service }}
  ansible.posix.firewalld:
    service: "{{ web_firewall_service }}"
    permanent: true
    immediate: true
    state: enabled
