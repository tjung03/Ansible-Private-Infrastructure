---
# 4) 방화벽 등록
# firewalld에 smtp, imap 서비스를 등록한다.
# firewalld 설치 및 기동은 별도 firewall role에서 처리한다.

- name: 4-1) firewalld MAIL 서비스 등록 - {{ item }}
  ansible.posix.firewalld:
    service: "{{ item }}"
    permanent: true
    immediate: true
    state: enabled
  loop: "{{ mail_firewall_services }}"
